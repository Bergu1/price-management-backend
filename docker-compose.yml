name: price-management
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args: [DEV=true]
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
      - dev-static-data:/vol/web
    command: >
      sh -c "python manage.py wait_for_db &&
             python manage.py migrate &&
             python manage.py runserver --noreload 0.0.0.0:8000"
    environment:
      - DB_HOST=db
      - DB_NAME=shopdb
      - DB_USER=devkonrad
      - DB_PASS=konradpass
      - MQTT_HOST=mqtt
      - MQTT_PORT=1883
      - MQTT_USER=backend
      - MQTT_PASS=backendpass
    depends_on:
      - db
      - mqtt
    restart: unless-stopped

  db:
    image: postgres:13-alpine
    volumes:
      - dev-db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=shopdb
      - POSTGRES_USER=devkonrad
      - POSTGRES_PASSWORD=konradpass
    restart: unless-stopped

  mqtt:
    image: eclipse-mosquitto:2
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto:/mosquitto/config
      - ./mosquitto/data:/mosquitto/data
      - ./mosquitto/log:/mosquitto/log
    restart: unless-stopped

  # --- Frontend (Vite/React) - DEV z hot reloadem ---
  frontend:
    build:
      context: ../price-management-frontend   # katalog frontendu (obok backendu)
      target: dev
    ports:
      - "5173:5173"
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
      - VITE_HMR_CLIENT_PORT=5173
    volumes:
      - ../price-management-frontend:/app     # ten sam katalog co w 'context'
      - /app/node_modules
    depends_on:
      - app
    restart: unless-stopped

volumes:
  dev-db-data:
  dev-static-data:
